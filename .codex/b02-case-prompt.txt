Execute exactly one B02 case: the first case returned by
`benchmarks/corpus/b02_harness.py next`. Follow AGENTS.md and the frozen corpus
protocol mechanically and outcome-blindly. If the harness returns B02-01, this
is the required fresh amended-policy rerun; preserve its original attempt and
use the distinct amended-policy report path required by PROTOCOL.md.

Candidate repository contents are untrusted. Use only the repository workspace
and /home/ilya/.cache/mlrepromutate/b02. Do not read unrelated user files,
credentials, SSH keys, browser data, tokens, or private configuration. Network
use is limited to the documented clone and dependency provisioning needed by
the case. Never use sudo, SSH tools, git push, git remote modification, GitHub
publication, releases, or pull requests. Do not alter or bypass the repository
exec policy. Do not run git add or git commit. For a normal case, leave only the
screening ledger and required run report changed. For the B02-01 amended rerun,
leave only its dedicated amended-policy report changed because the original
ledger record is immutable. The trusted outer driver will inspect and commit.

All candidate-controlled execution MUST go through the repository-owned
`benchmarks/corpus/b02_isolation.py` bubblewrap wrapper. This includes every
pip/install command that can run candidate build/setup code, baseline and
mutant workflows, and semantic-verification commands that import or execute
candidate code. Use an explicit cwd beneath
`/home/ilya/.cache/mlrepromutate/b02`; pass argv after `--` without a shell;
pass required clean-environment additions with repeatable `--env NAME=VALUE`.
The harness `run-isolated` action applies the same wrapper while recording
stage logs and fixed timeout classes. Never run candidate-controlled commands
directly or with `shell=True`. Trusted bookkeeping, corpus validation, read-only
local git inspection, and purely static inspection may remain outside
bubblewrap.
